[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = ['npm ci --only=production --no-audit --no-fund']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'npm start'